import 'package:flutter/material.dart';

class VierNullVier extends StatelessWidget {
  const VierNullVier({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("404"),
      ),
    );
  }
}
